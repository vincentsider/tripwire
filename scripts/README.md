# scripts

Utility scripts. The Supabase TypeScript types in `src/data/database.types.ts`
are generated from the live schema — regenerate with the Supabase MCP or:

    supabase gen types typescript --project-id cdmbaujqfnknilruuhht > src/data/database.types.ts
