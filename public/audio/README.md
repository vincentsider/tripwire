# Bundled audio

`synthetic-caller.webm` — a short AI-generated voice clip (a generic
text-to-speech stock voice, NOT a clone of any real person) used by level T7 to
demonstrate the deepfake detector. DeepBlocker's detector classifies it as FAKE
(≈0.9999). The clip says a benign phrase and carries no real identity.
